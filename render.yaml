services:
  - type: web
    name: diligence-mcp-ops
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: false
        value: 3000
    healthCheckPath: /health
